# curl-runner Remotion promo

Generate the curl-runner promotional video with Remotion + Bun.

## Scripts

- Preview: `bun run dev`
- Render MP4: `bun run render`
- Render thumbnail: `bun run still`

Outputs land in `packages/remotion/out`.
