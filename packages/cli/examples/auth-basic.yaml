# Basic authentication example
# Usage: curl-runner auth-basic.yaml

request:
  name: Basic Auth Request
  url: http://localhost:3000/api/test/auth/basic
  method: GET
  auth:
    type: basic
    username: user
    password: pass
