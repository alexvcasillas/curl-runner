# Simple single request example
global:
  output:
    verbose: true
    format: pretty

request:
  name: Get JSONPlaceholder User
  url: https://jsonplaceholder.typicode.com/users/1
  method: GET
  expect:
    status: 200
